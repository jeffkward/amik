"""Amik — a board an agent can work.

`amik/board.jsonl` holds card STATE, `amik/cards/<id>.md` holds card
PROSE, and `amik/amik.toml` says how this project is verified. The rest
is a reader, a writer, and the rules about which of them may touch what.
"""
