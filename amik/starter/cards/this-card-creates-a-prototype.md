the prototype toggle is on, so an agent writes a single page
and stops. you look at it before anything is built for real.

the brief is deliberately small — a tic-tac-toe board. three by three,
click to place, say who won. one file, no libraries.

turning the prototype toggle on also turns the pause on and locks it.
building a design pass and then merging whatever came out is not a
state anyone wants, so the card cannot be closed without you.

drag it to ready, or open it with the pencil and change status to
ready.
